package `in`.svnu.widgetclient.service

/**
 * Foreground service that manages floating overlay widgets.
 *
 * Responsibilities:
 * - Maintain persistent notification
 * - Manage WindowManager overlay views
 * - Observe Room WidgetState changes via Flow
 * - Handle tap-to-toggle on overlay emojis
 */
// TODO: Phase 6 — Implement FloatingWidgetService
