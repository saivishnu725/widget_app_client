package `in`.svnu.widgetclient.service

/**
 * BroadcastReceiver for BOOT_COMPLETED.
 *
 * On device boot: checks if user is authenticated and has
 * active overlays, then starts FloatingWidgetService.
 */
// TODO: Phase 6 — Implement BootReceiver
