package `in`.svnu.widgetclient.data.repository

/**
 * Repository for authentication operations.
 *
 * Wraps AuthApi calls, manages TokenManager for JWT storage,
 * and stores user info in DataStore.
 */
// TODO: Phase 2 — Implement AuthRepository
