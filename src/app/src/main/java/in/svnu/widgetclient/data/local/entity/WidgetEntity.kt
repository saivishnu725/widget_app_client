package `in`.svnu.widgetclient.data.local.entity

/**
 * Room entity representing a widget.
 *
 * Fields: id, name, emoji, ownerId, sharedUserIds,
 *         isOverlayActive, overlayPositionX, overlayPositionY,
 *         createdAt, updatedAt, lastSyncAt
 */
// TODO: Phase 3 — Implement WidgetEntity with @Entity annotation
