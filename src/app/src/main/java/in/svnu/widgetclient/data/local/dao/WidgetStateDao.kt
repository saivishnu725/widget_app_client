package `in`.svnu.widgetclient.data.local.dao

/**
 * Room DAO for widget state queries and updates.
 *
 * Operations: upsert, getByWidgetId, getAll
 * All queries return Flow for reactive UI updates.
 */
// TODO: Phase 3 — Implement WidgetStateDao with @Dao annotation
